glViewport(u, v, w, h);
glMatrixMode(           );
glLoadIdentity(); /// Reset matrix

double apparentHeight =             ;

// Setup projection matrix
if (                    ) {
    gluOrtho2D();
} else {
    gluOrtho2D();
}